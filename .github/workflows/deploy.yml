name: Deploy Frontend

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install SSH Key
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.GCP_SSH_KEY }}

      - name: Sync and Update Frontend
        run: |
          rsync -avz --delete \
            - e "ssh -o StrictHostKeyChecking=no" \
            --exclude='.git' \
            --exclude='node_modules' \
            ./ numa-desarrollo@34.71.107.222:/numa-reporteador-frontend
          
          ssh -o StrictHostKeyChecking=no numa-desarrollo@34.71.107.222 \
            "cd /numa-reporteador-frontend && ./update-service.sh frontend
